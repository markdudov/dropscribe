node_modules/
out/
release/
dist/
.dev/
vendor/bin/
*.log
.DS_Store
._*
.vite/
coverage/

# Incremental-build caches and hook state: regenerated by tooling, different on
# every machine and after every run. Tracking them put unrelated churn in the
# diff of every commit that happened to follow a typecheck.
*.tsbuildinfo
.claude/.docs-guard-snapshot.json
